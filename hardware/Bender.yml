package:
  name: mempool

dependencies:
  axi:                  { git: "git@github.com:pulp-platform/axi.git",                      version: 0.9.1   }
  cluster_interconnect: { git: "https://github.com/pulp-platform/cluster_interconnect.git", rev:     dev     }
  snitch:               { git: "git@iis-git.ee.ethz.ch:sriedel/snitch.git",                 rev:     mempool }

workspace:
  checkout_dir: "./deps"

sources:
  # Level 0
  - src/mempool_pkg.sv
  - src/snitch_address_demux.sv
  - src/tcdm_shim.sv
  # Level 1
  - src/mempool_tile.sv
  # Level 2
  - src/mempool.sv

  - target: mempool_test
    files:
      # Level 1
      - tb/soc_registers.sv
      # Level 2
      - tb/mempool_tb.sv
